import React from 'react';
import { Box, Grid, ButtonGroup, Button, Select, TextField } from "@material-ui/core";
import { StopIcon, PlayArrowIcon } from '@material-ui/icons';



const Toolbar = () => {

    //<Box> Wraps around content
    
    //Grid> Mobile view

    //variables here to set up this functionaslity of our sequencer

    //Functions for onClick events: start, play, BPM, Sequence Setter (target.e), 

    //<ButtonGroup>
    
    //<Button/> on click to play sequence<PlayArrowIcon></Button>
    
    //<Button/> on click to stop sequence <StopIcon> </Button>
    
    //</ButtonGroup>

    //<TextField> way to set BPM </TextField>

    //<Select> way to set and select a starting sequence </Select>

    //</Grid>
    //</Box>


    return (
        <div>
            
        </div>
    )
}

export default Toolbar;
