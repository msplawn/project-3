import React from 'react';
import {Box, Grid, Button, Select} from "@material-ui/core";
import { StopIcon, PlayArrowIcon} from '@material-ui/icons';



const Toolbar = () => {
    return (
        <div>
            
        </div>
    )
}

export default Toolbar;
